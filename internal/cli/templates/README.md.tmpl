# {{.Project}}

Generated by [gin-kit](https://github.com/Alfian57/gin-kit), a learning-first Go toolkit built on Gin.

## Start

```bash
gin-kit run
```

The server runs at `http://localhost:8080`.

The generated runtime includes request IDs, security headers, body limits,
trusted-proxy handling, graceful shutdown, SQL-backed readiness checks, and
in-memory endpoint-class rate limiting. Review `.env.example` before deploying.

## Commands

```bash
gin-kit run
gin-kit build
gin-kit check
gin-kit explain architecture
gin-kit generate resource tasks
gin-kit db up
```

When the Tasks example is enabled, the generated project includes a complete
domain, repository, service, and handler vertical slice. API projects expose
`/api/v1/tasks`; UI projects expose `/tasks`.

## Stack

- Mode: `{{.Mode}}`
- Database: `{{.Database}}`
- Data access: `{{.ORM}}`
- Authentication: `{{if .Auth}}enabled{{else}}disabled{{end}}`

Read `AGENTS.md` before asking an AI coding agent to modify this project.
