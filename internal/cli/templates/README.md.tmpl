# {{.Project}}

Generated by [GinKit](https://github.com/Alfian57/ginkit), a learning-first Go toolkit built on Gin.

## Start

```bash
ginkit run
```

The server runs at `http://localhost:8080`.

The generated runtime includes request IDs, security headers, body limits,
trusted-proxy handling, graceful shutdown, SQL-backed readiness checks, and
in-memory endpoint-class rate limiting. Review `.env.example` before deploying.

## Commands

```bash
ginkit run
ginkit build
ginkit check
ginkit explain architecture
ginkit generate resource tasks
ginkit db up
```

## Stack

- Mode: `{{.Mode}}`
- Database: `{{.Database}}`
- Data access: `{{.ORM}}`
- Authentication: `{{if .Auth}}enabled{{else}}disabled{{end}}`

Read `AGENTS.md` before asking an AI coding agent to modify this project.
