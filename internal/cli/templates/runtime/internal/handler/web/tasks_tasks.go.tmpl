package web

import "github.com/gin-gonic/gin"

func registerTasks(router *gin.Engine) {
	router.GET("/tasks", func(c *gin.Context) {
		c.String(200, "Tasks example: add your service and repository.")
	})
}
