package seeders

import (
	"context"

	"{{.DatabaseImport}}"
)

// Seed{{.Name}} seeds {{.Camel}} data. Keep it idempotent (check before
// inserting) and register it in All (seeders.go):
//
//	{Name: "{{.Snake}}", Run: Seed{{.Name}}},
//
// The connection exposes db.SQL plus the project ORM handle, so repositories
// and factories work here:
//
//	// repo := repository.New{{.Name}}Repository(db)
//	// _, err := factories.New{{.Name}}Factory().CreateMany(ctx, 10, repo.Create)
func Seed{{.Name}}(ctx context.Context, db *database.Connection) error {
	// Insert idempotent seed data here.
	return nil
}
